{
  "name": "node-to-do",
  "version": "1.0.0",
  "description": "This a simple application that uses nodejs as the backend, and allow users to create and modfiy to do list. It has authentication so that no users can access the other users to do list. Each To do list has a file attach to it.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ruijie Zhang",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "ejs": "^2.4.2",
    "express": "^4.14.0",
    "mongoose": "^4.5.3"
  }
}
